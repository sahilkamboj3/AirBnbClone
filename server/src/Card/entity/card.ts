export interface CardType {
    inside: number,
    mid: number,
    three: number,
    passing: number,
    steal: number,
    block: number,
    img: any,
}

export function buildMakeCard({ isValidStat }) {
    return function makeCard({ inside, mid, three, passing, steal, block, img }: CardType) {

    }
}